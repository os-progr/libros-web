# NPM Configuration for Production
# Omit dev dependencies in production
production=true
omit=dev

# Security settings
audit=true
audit-level=moderate

# Performance
prefer-offline=true
progress=false

# Cache settings
cache-min=86400
